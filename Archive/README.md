# DrasticDS_nx tuning archive

`Archive/` contains only final WillsitoGG revisions that were actually published and later superseded.

The current `DrasticDS_nx 1.0.9 – Direct Forwarder Fix` NRO is **not** duplicated here; it belongs only in the current GitHub Release.

## Archived revision

`DrasticDS_nx 1.0.9 - Direct Forwarder Fix_v1/`

This revision is recoverable exactly from the historical `WillsitoGG/NSW_Tunning` tag `drasticds-nx-1.0.9-direct-forwarder`. The historical tag contains both the exact published NRO and the corresponding source file, so this archive can preserve them without reconstruction.

No failed builds, intermediate experiments, CI outputs or logs belong in this directory.